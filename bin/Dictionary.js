// Dictionary interface documentation:
// url_tmpl: string - url template to form a complete url
// lookup(word_s: string): string - look up a word

export default {
	url_tmpl: '',
	lookup(word_s) {
		return word_s
	},
}
